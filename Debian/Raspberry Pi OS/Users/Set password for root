before accessing to root you have to type a password that you have to created
sudo passwd root

will create a new password for root

if you have done everything then you can disable root:

sudo passwd -l root

if you will need to reactivate your root you will need to reset the password
setting a new one with the previous command:
sudo passwd root
