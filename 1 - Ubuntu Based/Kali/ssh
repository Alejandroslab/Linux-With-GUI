systemctl status ssh

check first then:

systemctl enable ssh 
(to autostart)

finally:

systemctl start ssh





    adduser tdh 
	to add a user for remote login. Set a password and other info.
    
	usermod -aG sudo tdh
	
    

